import React from 'react';
import { MDBCardText } from 'mdb-react-ui-kit';

const FullMenu = ({ name }) => {
  return <MDBCardText className='FullMenuText'>{name}</MDBCardText>;
};

export default FullMenu;
